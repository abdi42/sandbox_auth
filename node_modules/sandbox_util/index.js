var sandbox_util = module.exports = {

  checkObject: function (obj,expectedKeys,callback){
    for(var i=0;i<expectedKeys.length;i++){
      var key = expectedKeys[i]
        if(!obj[key]){
          var err = key + " not provided"
          return callback(err);
        }
      }

    return callback(null);
  }

}
